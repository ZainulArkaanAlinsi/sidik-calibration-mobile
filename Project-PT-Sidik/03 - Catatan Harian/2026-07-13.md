# Monday, 13 July 2026

⬅️ [[Minggu 01 - Setup & Fondasi]] · 🏠 [[Dashboard]]

*(hari pertama)* | [[2026-07-14|Hari berikutnya →]]

**Minggu 1 Hari ke-1** — Setup & Fondasi

## Fokus Hari Ini
Hari pertama: infrastruktur kosong jadi ada titik mulai.

## Tugas Backend (PIC: Zainul)
- [x] Buat repo `asmo-api` (Laravel), setup `.env`, install dependency dasar.

## Tugas Mobile (PIC: Raihan)
- [x] Buat repo `asmo-mobile` (Flutter), setup project, putuskan state management (Riverpod/Provider).

## Target Akhir Hari (Definition of Done)
Kedua repo ada, bisa di-run lokal (`php artisan serve` & `flutter run`).

## Progress Hari Ini (isi manual sebelum pulang)
**Backend (`asmo-api`)**
- Repo Laravel 13 (PHP 8.3) jalan, sudah ada branch `main` + `develop`, remote ke GitHub.
- Dependency dasar: **Laravel Sanctum** (auth token buat mobile) — `HasApiTokens` sudah nempel di model `User`, migrasi `personal_access_tokens` ada, route `/api` aktif.
- `.env` dikonfigurasi: `APP_NAME="ASMO API"`, `APP_TIMEZONE=Asia/Jakarta`, `APP_LOCALE=id`, DB diarahkan ke **MySQL** (`asmo_db`).
- `config/app.php`: timezone dibikin env-driven (`env('APP_TIMEZONE', 'UTC')`) — sebelumnya di-hardcode `'UTC'` jadi setting di `.env` nggak kebaca.
- `README.md` diganti dari bawaan Laravel → README project: tech stack, setup lokal, konvensi API, ringkasan aturan bisnis, git workflow.
- **Bug ketemu & dibenerin:** request ke endpoint ber-`auth:sanctum` tanpa token balikin **HTTP 500** (`Route [login] not defined`) — middleware auth bawaan nyoba redirect ke halaman login yang nggak ada di project API-only. Fix di `bootstrap/app.php` (`redirectGuestsTo(fn () => null)`), sekarang balikin **401 JSON** (`{"message":"Unauthenticated."}`). Penting buat mobile: kalau nggak, tiap token expired app dapat 500 + stack trace, bukan 401 yang bisa dihandle.
- Database **MySQL 8.0.44** (`asmo_db`, utf8mb4) dibuat, `php artisan migrate` sukses — 10 tabel awal jadi: `users`, `sessions`, `cache`, `cache_locks`, `jobs`, `job_batches`, `failed_jobs`, `password_reset_tokens`, `personal_access_tokens`, `migrations`.
- Verifikasi jalan lokal: `php artisan serve` → `GET /up` = **200**, `GET /api/user` tanpa token = **401 JSON** (dites dengan & tanpa header `Accept: application/json`), koneksi DB dari app kebaca (`db=asmo_db`).

**Mobile (`asmo-mobile`)**
- Repo Flutter jalan, remote ke GitHub.
- **State management: Riverpod** (`flutter_riverpod ^3.3.2` sudah masuk `pubspec.yaml`).

**Vault**
- Vault Obsidian `Project-PT-ASMO/` di-track di repo `asmo-api` (di `asmo-mobile` di-gitignore) — biar cuma ada satu sumber kebenaran, nggak dobel & nggak diverge.

## Blocker / Catatan
- ✅ **DoD hari ini tercapai** — kedua repo ada & bisa di-run lokal.
- Catatan environment: yang megang port **3306 itu MySQL 8.0**, bukan XAMPP. XAMPP keinstall tapi MySQL-nya nggak bisa start (port bentrok). Jadi kalau nanti ada yang bingung kenapa `root` tanpa password nggak bisa connect — itu sebabnya. Kredensial ada di `.env` masing-masing (nggak di-commit, `.env` masuk `.gitignore`).
- Pembagian PIC: **Backend = Zainul**, **Mobile = Raihan** (sudah diisi di semua catatan harian).
- 🔄 **Menyusul — pembagian PIC ditukar mulai [[2026-07-14]]**: **Backend = Raihan**, **Mobile = Zainul**. Catatan hari ini sengaja nggak diubah karena ini rekaman histori: hari-1 backend memang dikerjain Zainul.
- Raihan sudah diundang jadi collaborator (akses write) di repo `asmo-api` & `asmo-mobile` — tinggal nunggu dia accept undangannya.
