# Thursday, 16 July 2026

⬅️ [[Minggu 01 - Setup & Fondasi]] · 🏠 [[Dashboard]]

[[2026-07-15|← Hari sebelumnya]] | [[2026-07-17|Hari berikutnya →]]

**Minggu 1 Hari ke-4** — Setup & Fondasi

## Fokus Hari Ini
Login end-to-end pertama kali kelar.

## Tugas Backend (PIC: Raihan)
- [x] ~~Endpoint auth: `/login`, `/refresh`, `/logout` + JWT middleware.~~ → **udah kelar duluan [[2026-07-14]]** karena mobile minta didahulukan. Pakai **Sanctum**, bukan JWT, jadi **`/refresh` nggak perlu ada** (token Sanctum nggak kadaluarsa). Middleware `role` juga udah jadi sekalian. Hari ini praktis kosong buat backend — pakai buat mulai nyicil master data (Minggu 2) atau bantuin mobile.

## Tugas Mobile (PIC: Zainul)
- [ ] Screen Login (3 state: loading, error kredensial, sukses) terhubung ke endpoint asli.

## Target Akhir Hari (Definition of Done)
Bisa login dari app pakai akun admin hasil seeder, token tersimpan.

## Progress Hari Ini (isi manual sebelum pulang)
- **Panel admin Filament ditutup total: dari 5 → semua 11 model kepegang.** Detail lengkap di [[../DAILY_REPORT|DAILY_REPORT.md]] (4 commit, 18 file, +902/−12 baris, test naik 146 → 148 hijau).
  - **Resource Sertifikat** (read-only): daftar + filter status, unduh PDF (stream dari disk privat), tombol terbitkan ulang buat yang gagal generate, badge angka gagal di menu. Sengaja **tanpa create/edit** — sertifikat lahir dari job approve, ngedit dari sini bakal mecahin ketertelusuran ke sesi asalnya.
  - **Resource Kategori Alat** (CRUD): kode auto-slug dari nama, repeater kolom worksheet, hitung jumlah alat per kategori.
  - **Halaman Pengaturan Organisasi** (singleton): edit identitas + akreditasi PT + upload logo ke `public/logos` — path yang sama yang dibaca job generate sertifikat buat kop PDF.
  - **Logo Sidik** dipasang di `brandLogo`/favicon panel admin, dan di kop halaman verifikasi QR publik.
- Beres-beres dev tooling: `barryvdh/laravel-ide-helper` kepasang (`require-dev`) — sebelumnya `php artisan ide-helper:generate` gagal karena package-nya belum ada. Symlink `storage` juga dibuat (`php artisan storage:link`) — tanpa ini preview logo di panel & URL `/storage/logos/*` 404.
- Semua diverifikasi end-to-end: panel bisa dibuka, login admin (`admin@asmo.test` / `rahasia123`) jalan, suite test tetap hijau.

## Blocker / Catatan
- 🔴 **Laravel Extra Intellisense** masih error di ekstensi VS Code — app-nya sendiri udah dibuktiin bersih di 6 subsistem (boot, route, view, config, DB, refleksi 11 model). Kemungkinan besar error sisi client extension, **butuh teks error persis** dari yang ngalamin biar bisa di-pin fix-nya.
- 🟡 Belum ada `CertificateFactory` — aksi unduh/retry sertifikat baru teruji lewat smoke render + test API, belum ada test Livewire khusus buat tabelnya. → **dikerjain [[2026-07-17]]**.
- 🟡 `worksheet_schema` (kolom `nama`/`satuan`) jadi kontrak de-facto lewat UI Kategori Alat, tapi belum ada kode yang benar-benar mengonsumsinya — perlu dikonfirmasi ulang pas layar input kalibrasi manual dibuat.
- ⚠️ Catatan harian ini (16 Jul) sempat ketinggalan diisi pas hari-H — baru dilengkapi retroaktif [[2026-07-17|besoknya]] dari `DAILY_REPORT.md`. Ke depan, isi bagian ini sebelum pulang, jangan nunda.
- ℹ️ **Ops:** `queue:work` wajib jalan biar approve → generate sertifikat & notifikasi jatuh tempo benar-benar keproses.
